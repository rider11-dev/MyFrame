1、前端excel导入导出
2、orm框架选择，EF吧
3、aop
4、action、controller，权限过滤 √
5、ef miniprofiler整体监视下sql，优化
	① 预生成视图，https://msdn.microsoft.com/zh-cn/library/cc853327.aspx
	② 查询时，NoTracking 
	③ 返回正确的数据量	
	④ 考虑手动打开数据库连接

6、模块管理完善√
7、findbyselector√
8、httpmodule理解
9、全局异常处理
10、删除模块前，校验是否挂了操作√
11、新增、修改时，automapper √
12、权限分配 √
	全局：初始化时，加载所有操作到缓存，供切换模块时加载操作列表用
	①操作列表选中状态，通过事件触发（查找角色操作权限，先从缓存中找，无则加载，设置缓存；设置列表选中状态）
	①点击角色，查找其模块和操作缓存，无则加载（只需一次加载），然后设置模块的选中状态；更新缓存
	②点击模块，查找对应操作缓存，无则加载

13、用模板方法模式，重构服务层，提炼公共代码

14、操作按钮实现权限控制 √

15、检查角色权限相关所有连接查询和删除，关联条件必须加上perType √

16、新增用户后，自动设置普通用户角色

17、用户权限获取，提取统一接口

18、用户上次登录时间（使用19操作日志）

19、操作日志

20、角色、模块、操作，添加是否系统字段，“系统”的不允许删除 √

21、用户基本信息、附加信息拆除

22、所有基本信息表，添加创建人、创建时间、最后修改人、最后修改时间 √

23、用户级权限分配

24、前端tab实现，id唯一性；tree扩展修改，不能用全局  √  帮助树改造 √

25、主页面tab页支持 √

26、登录验证码 √

27、二维码登录

28、AutoMapper改用EmitMapper √

29、管理员角色不能随便动
	管理员角色不能修改，管理员角色权限不能删
