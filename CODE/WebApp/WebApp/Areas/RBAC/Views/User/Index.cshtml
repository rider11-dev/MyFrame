@using WebApp.Extensions.Html
@using MyFrame.RBAC.Model
@{
    var opts = ViewData["Opts"] as List<Operation>;
}
<div class="row" style="margin-top:10px;">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">用户管理</h3>
            </div>
            <div class="box-body">
                <div id="toolbar" class="container" style="margin:4px 0px;">
                    <div class="row">
                        <div class="col-sm-5 btn-group">
                            @{
                                if (opts != null && opts.Count > 0)
                                {
                                    foreach (var opt in opts)
                                    {
                                        @Html.OptButton(opt)
                                    }
                                }
                            }
                        </div>
                        <div class="col-sm-2" style="padding:0px;">
                            <input type="text" id="txtSearch_UsrManage" name="UserName" class="form-control" placeholder="用户名" />
                        </div>
                        <div class="col-sm-1" style="padding-left:2px;">
                            <button id="btnSearch_UsrManage" type="button" class="btn btn-success">
                                <span class="glyphicon glyphicon-search">查询</span>
                            </button>
                        </div>
                    </div>
                </div>
                <table id="grid_UsrManage" class="table table-bordered table-striped table-hover" cellspacing="0"></table>
            </div>
        </div>
    </div>
</div>
<script src="~/libs/rbac/user/index.js"></script>
<script type="text/javascript">
    $(function () {
        usermanage.init({
            urlAdd: "@Url.Action("Add")",
            urlSearch: "@Url.Action("GetUserFullInfoByPage")",
            urlHelpRoles: "@Url.Action("GridHelp", "Role")",
        });
    });
</script>